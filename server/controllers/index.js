const express = require('express')
const router = express.Router()
/*
good example

const {controller} = require('./controllerName')
router.get('/api/endPoint/:item', controller)
*/
module.exports = router
